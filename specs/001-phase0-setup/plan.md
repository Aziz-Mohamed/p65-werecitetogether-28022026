# Implementation Plan: Branch Setup & Surgical Removals

**Branch**: `001-phase0-setup` | **Date**: 2026-03-02 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/001-phase0-setup/spec.md`

## Summary

Branch setup from Quran School commit `6018fdb`: surgically remove the work-attendance feature (GPS teacher check-in), update all branding from "Quran School" to "نتلو معاً / WeReciteTogether", add deprecation comments to `school_id` and `class_id` references in key files, remove location permissions from app config, and verify all 17 preserved feature directories and 5 route groups continue to work. No database schema changes needed — research confirmed all GPS columns are either absent from the attendance table or already nullable.

## Technical Context

**Language/Version**: TypeScript 5.9 (strict mode)
**Primary Dependencies**: Expo ~54, React Native 0.83, React 19, Expo Router v6, Supabase JS 2, i18next + react-i18next
**Storage**: Supabase PostgreSQL (remote) — no schema changes in this spec
**Testing**: Jest + React Native Testing Library; manual navigation of all role flows
**Target Platform**: iOS + Android (Expo managed workflow)
**Project Type**: Mobile (React Native / Expo)
**Performance Goals**: N/A — no new features; existing performance must not degrade
**Constraints**: All 17 preserved feature directories must remain fully functional; 5 route groups must remain navigable; no dead imports after work-attendance removal
**Scale/Scope**: ~25 files modified, 1 feature directory deleted (12 files), 3 route/screen files deleted, branding updated in 4 files

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### Pre-Research Gate (Phase 0)

| Principle | Status | Evidence |
|-----------|--------|----------|
| I. Single-Tenant, Program-Scoped | PASS | `school_id` references PRESERVED with deprecation comments (FR-014). Existing RLS policies using `get_user_school_id()` NOT deleted. No new tables — no `program_id` scoping needed yet. |
| II. Role-Based Access (7 Roles) | PASS | All 4 existing roles preserved. No role changes in this spec. New roles deferred to later specs. |
| III. TypeScript-First, Strict Mode | PASS | All modifications in TypeScript. No `any` types introduced. Removal only — no new code. |
| IV. Feature Colocation | PASS | Work-attendance removed as complete colocated feature. No cross-feature import violations introduced. |
| V. Logical CSS Only (RTL/LTR) | N/A | No layout changes. |
| VI. i18n Mandatory | PASS | Branding updates go through i18n system (en.json + ar.json). Translation keys preserved per FR-017. |
| VII. Supabase-Native Patterns | PASS | No schema changes. RLS policies preserved. No ORM introduced. |
| VIII. Minimal Animation | N/A | No animation changes. |
| IX. External Meeting Integration | N/A | No meeting integration in this spec. |
| Backward Compatibility | PASS | `school_id` DEPRECATED not deleted. Existing tables preserved. Existing RLS untouched. PRD Section 0.2 preservation rules followed. |

**Gate Result**: ALL PASS — proceed to research.

### Post-Design Gate (Phase 1)

| Principle | Status | Evidence |
|-----------|--------|----------|
| I. Single-Tenant, Program-Scoped | PASS | Research confirmed 33 RLS policies with `get_user_school_id()` remain untouched. Deprecation comments added (not code changes). |
| IV. Feature Colocation | PASS | Research confirmed work-attendance is self-contained. Only 5 external files import from it — all cleaned up. |
| VI. i18n Mandatory | PASS | Research inventoried all branding strings: 2 in en.json, 2 in ar.json, 1 in constants.ts. All updated through i18n. |
| VII. Supabase-Native Patterns | PASS | data-model.md confirms no schema changes needed. FR-011 already satisfied (attendance table has no GPS columns). |

**Gate Result**: ALL PASS — proceed to task generation.

## Project Structure

### Documentation (this feature)

```text
specs/001-phase0-setup/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0: codebase research findings
├── data-model.md        # Phase 1: schema audit (no changes needed)
├── quickstart.md        # Phase 1: developer quickstart guide
├── contracts/
│   └── README.md        # Phase 1: no new contracts (removal-only spec)
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
# Files to DELETE
src/features/work-attendance/           # Entire directory (12 files)
app/(admin)/work-attendance/index.tsx   # Admin monitoring route
app/(admin)/settings/location.tsx       # Geofence config screen (depends on work-attendance)
app/(admin)/teachers/[id]/work-schedule.tsx  # Work schedule screen (depends on work-attendance)

# Files to MODIFY (remove references)
app/(teacher)/(tabs)/index.tsx          # Remove GpsCheckinCard import + usage
app/(admin)/index.tsx                   # Remove work-attendance NavCard
app/(admin)/_layout.tsx                 # Remove Stack.Screen entries

# Files to MODIFY (branding)
app.json                                # Name, slug, scheme, bundle ID, permissions
src/i18n/en.json                        # common.appName, student.journey.shareBranding
src/i18n/ar.json                        # common.appName, student.journey.shareBranding
src/lib/constants.ts                    # APP_NAME constant

# Files to MODIFY (deprecation comments)
src/features/schools/index.ts           # Module-level deprecation (FR-016)
src/features/reports/services/teacher-reports.service.ts
src/features/reports/services/admin-reports.service.ts
src/features/dashboard/services/admin-dashboard.service.ts
src/features/scheduling/services/scheduled-session.service.ts
src/features/scheduling/services/class-schedule.service.ts
src/features/scheduling/services/recitation-plan.service.ts
src/features/sessions/services/sessions.service.ts
src/features/sessions/services/checkin.service.ts
src/features/memorization/services/recitation.service.ts
src/features/memorization/services/assignment.service.ts
src/features/memorization/services/memorization-progress.service.ts
src/features/classes/services/classes.service.ts
src/features/attendance/services/attendance.service.ts
src/features/schools/services/school-settings.service.ts
src/features/scheduling/types/recitation-plan.types.ts
src/features/memorization/types/memorization.types.ts
src/features/auth/types/auth.types.ts
src/features/students/types/students.types.ts
src/features/sessions/types/sessions.types.ts
src/features/attendance/types/attendance.types.ts
src/features/children/services/children.service.ts
src/features/reports/services/parent-reports.service.ts
src/features/dashboard/services/teacher-dashboard.service.ts
src/features/gamification/services/gamification.service.ts
supabase/migrations/00001_consolidated_schema.sql  # RLS policy deprecation comments
```

**Structure Decision**: Existing Expo Router mobile app structure. No new directories created. Feature removal follows the colocation principle — entire `work-attendance/` directory deleted. Route files deleted match the feature. Modifications are surgical edits to existing files.

## Complexity Tracking

No constitution violations. All changes are additive (deprecation comments) or subtractive (feature removal). No new patterns or abstractions introduced.
